{
  "name": "worldmonitor-news-backend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Live news ingestion for World Monitor — GDELT + USGS + CryptoPanic normalized to {cat,title,place,lon,lat,t}, served over /api/events and /api/stream (SSE).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "engines": { "node": ">=18" },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2"
  }
}
